# Infrastructure scripts

Operational scripts for safe install, migration, backup validation and release tasks live here. Scripts must be idempotent where practical, fail closed, and never embed production credentials or modify MARBO3A resources implicitly.

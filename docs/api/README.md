# API documentation

REST and realtime contracts for 3AKSA are documented here as modules are implemented. All externally supplied data must be validated at runtime and authorization must be enforced server-side.

Default development routing is configuration-driven; examples may use `/3aksa/api`, but the application must remain portable to a root-domain deployment.

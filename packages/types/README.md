# @3aksa/types

Shared TypeScript-only contracts that do not require runtime validation. Security-sensitive input/output boundaries should use `@3aksa/schemas` rather than trusting compile-time types alone.

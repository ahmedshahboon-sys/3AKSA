# @3aksa/realtime

Shared Socket.IO event contracts and realtime client helpers. Authorization and block filtering remain server-side. Future WebRTC/SFU media signaling must stay logically separated from the V1 chat realtime layer.
